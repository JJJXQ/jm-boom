export type ReaderSearch = {
  title: string
  chapter: string
  albumId: string
  fromDetail: string
  nextId: string
  nextChapter: string
}

export type ReaderNextChapter = {
  id: string
  title: string
}
